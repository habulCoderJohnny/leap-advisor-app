import React from 'react';

const CustomButton = () => {
    return (
        <div>
            <button className="btn btn-warning rounded-full p-4">Get Started</button>
        </div>
    );
};

export default CustomButton;